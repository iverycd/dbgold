# 前端开发环境变量示例
# 复制为 .env.local 后按需修改（.env.local 已被 gitignore 忽略）。
# 仅影响 vite dev server 的 /api 代理目标，不影响生产构建（生产同源，不走代理）。

# 后端地址，需与 dbgold 根目录 .env 的 PORT 保持一致（默认 8080）
VITE_API_TARGET=http://localhost:8080
