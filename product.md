# dbgold 产品白皮书

## 1. 产品概述

dbgold 是面向异构数据库迁移与同步场景的可视化管理平台。它将数据库连接、迁移任务、实时监控、结果核验和协作审批整合在统一 Web 入口中，帮助企业更稳定、更透明地完成数据库迁移项目。

产品面向业务用户、数据库管理员和技术决策者，适用于数据库替换、国产化改造、系统升级、测试环境复制和数据迁移验证等场景。

## 2. 核心功能

### 2.1 连接管理

- 统一维护源库和目标库连接信息。
- 支持连接测试、数据库/Schema 列表获取和环境标识管理。
- 支持多种主流数据库及国产数据库，减少重复配置工作。

### 2.2 全量迁移

支持单次数据库迁移，可按全库或指定表选择迁移范围，并按需要选择迁移内容：

- 表结构与数据
- 仅表结构
- 仅数据
- 主键、索引、序列、外键和注释等数据库对象
- 视图迁移

迁移过程支持并发参数、分页参数、连接池参数和名称大小写等选项，适应不同规模与兼容性要求。

### 2.3 增量迁移

提供全量 + CDC 增量迁移能力，也支持从指定同步位点开始增量同步。系统记录任务状态、同步位点、事件统计、排除范围和异常信息，便于迁移切换前后的核对与闭环管理。

### 2.4 批量迁移

支持通过 Excel 模板导入多个迁移任务，统一提交并跟踪批次进度，适合多库、多环境或多租户迁移场景。系统对常见数据库名称和别名进行识别，降低批量录入成本。

### 2.5 迁移监控与报告

- 通过实时日志查看任务阶段、执行进度和异常信息。
- 提供迁移历史、任务详情和批次查询。
- 按表、数据、主键、视图、索引、外键、序列、注释和行数等维度展示结果。
- 对失败对象提供明细定位，支持进一步处理和复核。

### 2.6 SQL 查询中心

提供面向已配置连接的 SQL 查询能力，并集成 SQL 编辑体验，方便管理员进行数据核验、迁移前检查和迁移后验证。

### 2.7 例程与触发器导出

支持对 MySQL、Oracle、SQL Server 和达梦中的函数、存储过程、程序包及触发器进行原始 SQL 导出。

该能力坚持保留源数据库方言，不进行未经验证的跨数据库自动改写。用户可在目标数据库环境中人工适配后执行，降低错误转换带来的风险。

### 2.8 工单与协作

- 支持提交数据库迁移工单。
- 管理员可查看、编辑、处理和审批工单。
- 支持从工单信息一键创建连接并进入迁移流程。
- 记录需求说明、处理状态和处理备注，形成可追溯的协作链路。

### 2.9 用户与审计

提供登录认证、角色控制、管理员专属功能和登录历史记录。普通用户与管理员的操作范围有所区分，适合在团队环境中进行权限管理和使用审计。

## 3. 数据库支持范围

当前系统已接入以下数据库类型：

| 类型 | 产品名称 |
|---|---|
| mysql | MySQL |
| postgres | PostgreSQL |
| oracle | Oracle |
| sqlserver | SQL Server |
| gaussdb | GaussDB / openGauss 生态 |
| dameng | 达梦 |
| seabox | SeaBox |
| highgo | HighGo 瀚高 |
| vastbase | Vastbase 海量数据库 |
| gbase | GBase（PG 兼容） |
| kingbase | Kingbase 人大金仓 |
| oscar | Oscar 神通数据库 |

上述类型表示系统已实现连接或驱动接入。实际可执行的迁移能力取决于源库、目标库组合及对应对象类型，用户应以迁移页面显示的支持情况为准。

### 3.1 数据迁移方向

下表列出当前系统已实现的数据迁移组合。该表不代表所有对象类型、视图迁移或增量迁移均支持。

| 源库 | 支持的目标库 |
|---|---|
| MySQL | PostgreSQL、GaussDB、SeaBox、达梦、HighGo、Vastbase、GBase、Kingbase、MySQL、Oscar |
| SQL Server | PostgreSQL、GaussDB、SeaBox、HighGo、Vastbase、GBase、Kingbase、MySQL、Oscar |
| 达梦 | PostgreSQL、GaussDB、SeaBox、HighGo、Vastbase、GBase、Kingbase、MySQL、Oscar |
| Oracle | PostgreSQL、GaussDB、SeaBox、HighGo、Vastbase、GBase、Kingbase、MySQL、Oscar |
| PostgreSQL | 达梦 |
| GaussDB | 达梦 |
| HighGo | 达梦 |
| Vastbase | 达梦 |
| GBase | 达梦 |
| SeaBox | 达梦 |
| Kingbase | 达梦 |

### 3.2 全库/全量迁移

| 源库类型 | 全库/全量迁移说明 |
|---|---|
| MySQL | 支持，可选择全库或指定表 |
| SQL Server | 支持，可选择全库或指定表 |
| Oracle | 支持，可选择全库或指定表 |
| 达梦 | 支持，可选择全库或指定表 |
| PostgreSQL | 支持，以 Schema 作为迁移单元 |
| GaussDB | 支持，以 Schema 作为迁移单元 |
| HighGo | 支持，以 Schema 作为迁移单元 |
| Vastbase | 支持，以 Schema 作为迁移单元 |
| GBase | 支持，以 Schema 作为迁移单元 |
| SeaBox | 支持，以 Schema 作为迁移单元 |
| Kingbase | 支持，以 Schema 作为迁移单元 |

MySQL、SQL Server、Oracle 和达梦支持列举源库中的数据库或 Schema；PostgreSQL 及其兼容库以连接数据库内的 Schema 作为迁移单元。实际能否执行迁移，还需同时满足源库到目标库的支持组合。

### 3.3 增量迁移

当前增量迁移基于 MySQL Binlog CDC，支持以下源库到目标库组合：

| 增量源库 | 增量目标库 | 支持模式 |
|---|---|---|
| MySQL | PostgreSQL | 全量快照后持续同步、从指定 Binlog 位点开始 |
| MySQL | GaussDB | 全量快照后持续同步、从指定 Binlog 位点开始 |
| MySQL | HighGo | 全量快照后持续同步、从指定 Binlog 位点开始 |
| MySQL | Vastbase | 全量快照后持续同步、从指定 Binlog 位点开始 |
| MySQL | GBase | 全量快照后持续同步、从指定 Binlog 位点开始 |
| MySQL | Kingbase | 全量快照后持续同步、从指定 Binlog 位点开始 |
| MySQL | SeaBox | 全量快照后持续同步、从指定 Binlog 位点开始 |

“全量快照后持续同步”会先完成初始化快照，再进入 CDC 持续同步；“从指定 Binlog 位点开始”支持 GTID 或 Binlog 文件/位置方式。增量迁移需要满足源库 Binlog、账号权限、日志格式、位点保留时间和目标表结构等前置条件。

## 4. 产品亮点

### 面向异构与国产化迁移

覆盖传统商业数据库、开源数据库和多种国产数据库，尤其适合 PostgreSQL 兼容数据库生态下的迁移与替换项目。

### 全量与增量一体化

从一次性全量复制到基于 CDC 的持续同步，统一管理任务、日志、位点和切换前核验，支持更完整的迁移闭环。

### 过程可观测、结果可追溯

实时日志、任务状态、对象级结果和行数校验共同构成可核验的迁移记录，便于快速发现失败对象并开展复盘。

### 对象粒度清晰

表、数据、视图、主键、索引、序列、外键和注释等对象可分别处理，满足不同迁移阶段和不同兼容性要求。

### 原始导出优先

跨厂商函数和存储过程不强行自动转换，而是提供保留源方言的 SQL 导出，为人工适配保留完整上下文。

### 轻量部署与统一入口

前后端由 Go 服务统一提供，部署后通过一个 HTTP 入口访问 Web 界面和 API，降低部署与维护复杂度。

### 安全与运维能力内置

集成 JWT 认证、角色权限、验证码、登录历史、健康检查、备份恢复和升级回滚等基础能力，适合受控的企业内网部署。

## 5. 典型使用流程

1. 在连接管理中配置源库和目标库，并完成连接测试。
2. 选择迁移类型、源库数据库、目标库 Schema 和迁移范围。
3. 选择需要迁移的表、数据和数据库对象，配置必要的并发与兼容性选项。
4. 启动迁移任务，通过实时日志查看执行阶段和进度。
5. 查看迁移报告、失败对象和数据校验结果。
6. 对失败对象进行复核；对于函数、存储过程等例程，必要时导出原始 SQL 并人工适配。
7. 在历史任务中保留过程记录，便于后续审计和问题追踪。

对于大批量迁移，可先通过 Excel 导入任务批次，再统一查看批次及子任务状态。对于需要审批的场景，可先提交工单，由管理员处理后进入迁移流程。

## 6. 技术架构与技术栈

### 后端

- Go 1.25.5
- Gin Web 框架
- GORM 数据访问层
- SQLite 本地持久化
- JWT 身份认证
- SSE 实时推送任务日志与进度

### 前端

- Vue 3
- TypeScript
- Vite
- Arco Design Vue
- Pinia
- Axios
- CodeMirror SQL 编辑器

### 数据库接入与迁移引擎

- 使用 MySQL、PostgreSQL 及兼容数据库、Oracle、SQL Server、达梦和 openGauss 等对应驱动。
- 通过独立 Driver、Reader、Writer 和类型映射机制适配不同数据库方言。
- 采用并发任务执行、分页读写、连接池配置和持久化日志支撑迁移任务运行。
- 通过 CDC 与同步位点实现增量数据捕获和持续同步。

## 7. 部署与运维概览

dbgold 支持 Linux、Windows 和 Docker 部署。生产环境通常通过 HTTPS 网关对外提供访问，应用自身提供统一 HTTP 服务入口和健康检查接口。

主要运维能力包括：

- Linux Docker 安装、升级和回滚
- Windows 服务安装与自动启动
- 配置化端口、监听地址和数据目录
- 存活检查与就绪检查
- SQLite、上传文件、日志和配置数据的备份恢复
- 升级前运行任务检查
- 应用日志与迁移任务日志分离保存

当前产品定位为单机单实例部署，适合受控内网或专用迁移服务器场景。正式对外访问时建议通过 HTTPS 网关接入，并结合网络访问控制和数据库最小权限策略使用。

## 8. 使用边界

- 不同数据库组合的支持范围并不完全相同，应以系统实际提示和迁移选项为准。
- 函数、存储过程和程序包等跨厂商对象不进行保证正确的自动语法转换，通常需要人工适配。
- 迁移前应确认目标端权限、目标表数据保留要求和源库同步位点条件。
- 产品当前不以多副本、高可用集群或无停机切换为目标，生产升级应安排维护窗口。

通过将迁移执行、过程监控、结果核验和协作审批集中在一个平台中，dbgold 为数据库迁移项目提供了一套清晰、可控、可追溯的工作方式。
